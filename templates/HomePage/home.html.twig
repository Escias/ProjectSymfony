<!DOCTYPE HTML>
<html>
<head>
    <meta charset="utf-8">
</head>
<body>
<h1>Home Page</h1>
<ul>
    {% for team in teams %}
        <li>
            <a href="/team/{{ team.id }}">{{ team.name }}</a>
            <p>Number of projects: {{ team.listProject|length }}</p>
            <a href="/team/delete?id={{ team.id }}">Supprimer</a>
        </li>
    {%  endfor %}
</ul>

{{ form(formAdd) }}
</body>
</html>


<style>
    h1{
        text-align: center;
        color: deeppink;
        font-size: 150px;
    }
</style>